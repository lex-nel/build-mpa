.filter-wrapper {
  display: block;
  max-width: 100%;
  overflow: hidden;
  width: 100%;

  &__list {
    column-gap: 30px;
    flex-wrap: wrap;
    row-gap: 16px;

    @apply flex flex-row items-start justify-start;

    > * {
      font-weight: 600;
      white-space: nowrap;
    }
  }
}
